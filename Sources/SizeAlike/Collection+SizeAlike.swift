import Foundation

public extension Collection {
    /// Alias for `count`. Provided by SizeAlike.
    var size: Int { count }

    /// Alias for `count`. Provided by SizeAlike.
    var length: Int { count }
}
