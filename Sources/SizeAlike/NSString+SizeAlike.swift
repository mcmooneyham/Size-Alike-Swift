import Foundation

public extension NSString {
    /// Alias for `length`. Provided by SizeAlike.
    var size: Int { length }

    /// Alias for `length`. Provided by SizeAlike.
    var count: Int { length }
}
